import { Colors } from 'src/app/commons/models/colors';

export class VaccineSupplierColors {

    public static SUPPORTED = [
        Colors.SUPPORTED[1],
        Colors.SUPPORTED[2],
        Colors.SUPPORTED[4]
    ];
}
